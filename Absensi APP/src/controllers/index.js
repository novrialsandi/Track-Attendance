const userController = require("./user");
const compController = require("./company");
const attlogController = require("./attendancelog");
module.exports = {
  userController,
  compController,
  attlogController,
};
